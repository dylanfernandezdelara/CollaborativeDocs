{
  "name": "collaborative-docs",
  "install": "npm ci && CONVEX_AGENT_MODE=anonymous npx convex dev --once",
  "terminals": [
    {
      "name": "convex",
      "command": "CONVEX_AGENT_MODE=anonymous npx convex dev",
      "description": "Local anonymous Convex backend. Syncs schema/functions and keeps NEXT_PUBLIC_CONVEX_URL current in .env.local."
    },
    {
      "name": "next",
      "command": "npm run dev -- --hostname 0.0.0.0 --port 3000",
      "description": "Next.js app on port 3000. Depends on Convex terminal / .env.local from install."
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 3000
    }
  ]
}
